// StudentPortalView.tsx — thin re-export wrapper for ParentView
// The underlying data model uses parentPassword, which is the student login credential.
// All tabs and display logic are preserved from ParentView.
export { default } from "./ParentView";
